# Addons

This directory contains editor/development plugins used by the template.

godot_mcp/ is the current Godot MCP bridge. It is enabled in project.godot,
but is excluded from Web exports because it is a development tool and is not
needed at runtime.
